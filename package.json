{
  "name": "lea-market-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clear": "rm -rf node_modules .nuxt package-lock.json",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm i && npm run build && npm run generate && firebase deploy --only hosting:lea-market-docs"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.9.0",
    "nuxt": "^2.15.2"
  }
}
