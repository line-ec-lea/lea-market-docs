{
  "name": "lea-market-docs",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "clear": "rm -rf node_modules .nuxt yarn.lock",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "pre-build": "yarn build && yarn generate"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.9.0",
    "nuxt": "^2.15.2"
  }
}
