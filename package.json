{
  "name": "lea-market-docs",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "clear": "rm -rf node_modules .nuxt package-lock.json",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "pre-build": "npm run build && npm run generate"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.9.0",
    "dotenv": "^12.0.3",
    "nuxt": "^2.15.2"
  },
  "devDependencies": {
    "@nuxtjs/google-analytics": "^2.4.0"
  }
}
